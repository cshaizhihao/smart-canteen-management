<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>注册 - 校园智慧食堂</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="auth-box">
        <h1>新用户注册</h1>
        <form action="/register" method="POST">
            <input name="username" placeholder="设置用户名" required>
            <input name="password" type="password" placeholder="设置密码" required>
            <button type="submit">完成注册</button>
        </form>
        <% if(error) { %>
            <p style="color:#e63946; margin-top: 15px;"><%= error %></p>
        <% } %>
        <p style="margin-top: 20px; font-size: 0.9rem;"><a href="/login" style="color: var(--primary-color);">返回登录</a></p>
    </div>
</body>
</html>
